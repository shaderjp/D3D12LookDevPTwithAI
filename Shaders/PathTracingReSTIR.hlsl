// Artifact name retained for project/UI compatibility. RTXDI ReSTIR PT/GI is
// a separate quality gate, so the GI-only mode intentionally remains on the
// Baseline MIS path while the DI and Combined variants enable RTXDI DI.
#define PT_RESTIR 0
#include "PathTracingABI.hlsli"
